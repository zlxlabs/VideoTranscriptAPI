# 可观测性注入文件（示例）。真实 ops.env 只放在部署目录、绝不进 git。
#
# 用途：docker-compose.yml 通过 env_file 把这里的变量注入容器 os.environ，
# 供 zlx-ops-sdk 上报错误到 GlitchTip。DSN 绝不写进 config/config.jsonc。
#
# 部署步骤（n305）：
#   1. 在 GlitchTip 建 project 拿到 DSN
#   2. 复制本文件为部署目录下的 docker/ops.env
#   3. 填入真实 SENTRY_DSN
#   4. docker compose up -d

# GlitchTip DSN（缺省 = 不上报，SDK fail-open，不报错）
SENTRY_DSN=
