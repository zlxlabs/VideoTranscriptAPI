# @generated by docker-package skill
# @skill-version 1
services:
  video-transcript-api:
    image: ghcr.io/zj1123581321/video-transcript-api:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: video-transcript-api
    environment:
      - TZ=Asia/Shanghai
    # 可观测 DSN 只经 env_file 注入 os.environ（不挂载、不进 git）。
    # 缺 ops.env 也能起（SDK fail-open）。DSN 绝不写进 config.jsonc。
    env_file:
      - path: ops.env
        required: false
    ports:
      # 宿主端口 8200（n305 上 portainer 已占 8000）；外部入口
      # https://sum.lexgogo.site 经 Cloudflare Tunnel 落到 :8200。
      - "8200:8000"
    volumes:
      # Configuration files (user must prepare before starting)
      - ../config:/app/config
      # Persistent data: cache, logs, transcription results
      - ../data:/app/data
    mem_limit: 2g
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
